# Simple Entity Component System

Secs is a small, header-only ECS library meant to be as simple as possible.

### ECS Architecture

Secs is a Sparse Set ECS, which means

Components are the central piece on which Entities and Systems are built.

TODO: More detail on this later.

Star this library to support development!